#include <iostream>
using namespace std;
int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int t;
    cin >> t;
    while (t--) {
        int n, m, q;
        cin >> n >> m >> q;
        int tc[m];
        for (int i = 0; i < m; i++) {
            cin >> tc[i];
        }
        for (int i = 0; i < q; i++) {
            int dq;
            cin >> dq;
            
        }

    }
}
#include <iostream>

using namespace std;

void combine_out(vector< vector <vector<int> > > charset) {
    /*
    Argument:
    charset: 3D vector of integers, where each 2D vector in a vector represents a character
    */
    int max_width=0;
    int max_height=0;
    for(int char_number = 0; char_number < charset.size(); char_number++) {
        
    }
}